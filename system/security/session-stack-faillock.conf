# Keep lock-screen failures separate from sudo and TTY login. An interrupted
# password conversation must not make the account's recovery routes unusable.
dir=/run/session-stack-faillock
deny=3
fail_interval=900
unlock_time=600
silent
